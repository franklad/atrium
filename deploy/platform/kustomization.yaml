apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - cluster-issuer.yaml
  - wildcard-cert.yaml

labels:
  - pairs:
      app.kubernetes.io/part-of: atrium
    includeSelectors: false
